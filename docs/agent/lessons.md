# Agent lesson ledger

Keep only cross-task lessons here. Task files retain detailed evidence.

| ID | Status | Scope | Lesson | Evidence | Promotion / next trigger |
| --- | --- | --- | --- | --- | --- |
| L001 | candidate | Toolchain migration | When changing JavaScript package managers, an existing install tree may need a clean reinstall before the new manager can generate a reliable lockfile. | T13 npm migration: npm failed against the pnpm-created `node_modules`, then lock generation and all frontend gates passed after removing only that generated cache. | Promote to a toolchain migration skill/checklist if the repository changes manager again or the same failure recurs. |
| L002 | verified | Environment audit | Distinguish tools supplied by the agent runtime from tools available in the developer's normal shell before declaring prerequisites satisfied. | T13 environment audit found different Node/npm visibility and no system `dotnet`; README was corrected to reflect the developer setup. T27: `npm run e2e` assumed `pwsh`, which the developer machine lacks. | Verified by recurrence; README states the Windows PowerShell 5.1 invocation. Promote to an environment audit checklist on the next bootstrap task. |
| L003 | promoted | Verification scripts | PowerShell must check `$LASTEXITCODE` after each native npm/.NET command; `$ErrorActionPreference = 'Stop'` alone does not make a nonzero native exit fail the script. | T03: `npm ci` failed with `EPERM` while Vite held a native binding; subsequent commands failed, yet the old `verify.ps1` returned 0 after a successful .NET test. The amended script failed immediately on a reproduced npm error, then passed end-to-end after Vite stopped. | Encoded in `scripts/verify.ps1`; revisit if verification commands or shell semantics change. |
| L004 | verified | SQLite adapter timestamps | Keep UTC timestamps in domain/application, but map fields used for SQLite ordering or expiry predicates to provider-supported UTC `DateTime` values in Infrastructure; EF Core SQLite does not translate `DateTimeOffset` ordering/comparison. | T04 migration initially generated `DateTimeOffset` expiry columns; the [EF Core SQLite limitations](https://learn.microsoft.com/en-us/ef/core/providers/sqlite/limitations) confirm the query limitation. Adapter switched to UTC `DateTime`, migration was regenerated, and live SQLite tests passed expiry, revision and resume queries. | Reuse when extending session expiry or adding provider adapters; promote to a storage-specific checklist only after T12 portability work supplies a second context. |
| L005 | verified | React–Phaser overlay keyboard handoff | Check scene commands issued before Phaser `create`, and ensure a modal's Escape close does not also toggle canvas pause during the same key event. | T05 real Chrome path exposed a pre-create input exception and double Esc handling; guarded scene input and capture-phase overlay key handling fixed it. T06 visible Chrome test repeated notebook Escape → canvas focus → movement → Maya interaction and passed after the question overlay was added. | Keep the narrow product fix; consider an automated overlay/keyboard regression test if T07 adds another modal or changes key handling. |
| L006 | promoted | React StrictMode and Phaser lifecycle | Treat canvas ownership, player position, held input and DOM focus as one session/remount boundary. Synchronously clear the owned parent, key/remount the host when session identity changes, focus the new canvas only when no overlay is open, regression-test StrictMode/replay, and disable audio only while the product has no audio. | T07 visible Chrome reload exposed duplicate canvases and closed `AudioContext` exceptions. T08 replay exposed stale meeting-zone position in a fresh session. T09 fresh-session preview exposed missing canvas focus; the GameHost focus fix and test restored immediate keyboard ownership. Each final browser run retained one canvas. | Encoded in `GameHost`, its lifecycle tests, and `apps/web/AGENTS.md`. Reuse in T10 E2E; retire the `noAudio` clause when audio becomes a feature. |
| L007 | promoted | Windows local integration runners | Quote child-process paths containing spaces, set each service's working/content directory explicitly, reject occupied ports, wait through the browser-facing proxy, and clean only runner-owned PIDs plus validated unique data paths. | T10 initially split the API/Vite paths, then failed to load API settings without its content root; a later direct-health success preceded proxy readiness. The amended runner rejected an occupied port and repeatedly completed the isolated headed journey with cleanup. | Encoded and validated in `scripts/e2e.ps1`; reuse for T12 storage round-trip and extend only when another local service joins the stack. |
| L008 | promoted | Windows E2E prerequisites | Before the headed E2E runner, check that `pwsh` exists (else call `scripts/e2e.ps1` through Windows PowerShell 5.1) that no developer API process holds `services/api/bin` open, and that no preview Vite server holds `node_modules` before `verify.ps1`; ask before stopping a user process. | T27 V1: `npm run e2e` failed with `pwsh` not recognized; then MSB3027 file lock from the running API (PID 5848). Passed after the approved stop. V5: `verify.ps1` `npm ci` EPERM from a running preview Vite; passed after stopping it. | Promoted in T11 (same failure family as L003 T03 EPERM): preflight in `docs/memory/current.md` "Verification commands" and README troubleshooting. Automate in the runners if it recurs after T11. |
| L009 | promoted | Phaser asset loading | A missing static asset may return an SPA `index.html` with status 200; Phaser's SVG loader then throws uncaught instead of emitting a load error, blanking the scene. Fetch as text, validate the document type, then load; test the missing-file path in the real browser. | T27 V2: removing `desk.svg` produced a blank canvas and `SVGFile.onProcess` `hasAttribute` TypeError; the validated loader rendered the fallback and notice. | Promoted in T27 V3 to `apps/web/AGENTS.md` after character sheets reused the loader and their missing-file fallback passed in the browser. |
| L010 | verified | Visual QA of late-game UI | On the Vite dev server, `await import('/src/api/...')` in the visible page and call the real API functions to prepare case state, then inspect the UI; answers come from the server case file, never from frontend code. Use only against a local dev database. | T27 V4: prepared E01–E06, Q01–Q03 and the conclusion in seconds to inspect notebook, evidence board, result and review. V5 reused it to reach the meeting checkpoint for grayscale inspection. | Verified by repeat. Keep as a ledger note; promote to a QA script only if a third UI task needs it. |
| L011 | candidate | Bundler configuration | The headed E2E runner uses the Vite dev server, so after changing aliases, `define`, chunking or other bundler config, also load the production build (`vite preview` on 127.0.0.1:5173) in the visible browser. Flags for CommonJS dependencies must also go to `optimizeDeps.rolldownOptions.transform.define`; an undefined `typeof FLAG` is the truthy string "undefined". | BUFFER Phaser split: production blank page (`global is not defined`) passed all scripts; then dev E2E 0/4 from `require('phaser3spectorjs')`. Shared flag map fixed both. | Promote to an E2E production-bundle mode or rule if bundler config changes again. |
| L012 | promoted | E2E on slow CI runners | Hosted runners render without a GPU (~29 fps vs 60 locally). E2E steps must wait for outcomes (dialog visible, counter increased) instead of fixed hold times, damp keyboard movement (halve hold on overshoot, minimum hold > one slow frame, several stuck presses before "blocked"), target inside reachable floor rather than exactly at collider boundaries while accounting for player radius/frame movement, set the intended direction before a dodge that uses the last movement vector, keep hazard clearance, and surface failures as public `::error` annotations because job logs need sign-in. | P02 runs #2–#12: movement budget, 180 s timeout, oscillation, fixed scanner walk, table clip, 300 ms dodge window, scanner-edge tolerance — each fixed; run 36213362487 green. P03 run 36234356486 exposed the y=495 cabinet boundary target stopping at y=503.308; retargeting the safe lane to y=500 passed local headed 4/4 and remote run 36235584949. Run 36236947169 exposed the retained up-dodge vector pushing into the cabinet edge plus a fixed-hold visual movement assertion; set rightward movement before dodge and poll actual movement instead, both passing local headed 4/4. | Encoded in `apps/web/AGENTS.md`; revalidate on future slow-runner movement failures or when route/collider/dodge behavior changes. |
| L013 | verified | Windows Docker smoke invocation | In Windows PowerShell, `bash` may resolve to a WSL shim that fails because `/bin/bash` is unavailable; invoke Git for Windows' `bin/bash.exe` explicitly or run from Git Bash. | P03: default `bash scripts/docker-smoke.sh` failed with `/bin/bash` unavailable; the same smoke passed using `C:\Program Files\Git\bin\bash.exe`. | Recorded in `docs/runbooks/github.md`; reuse for local Docker smoke on Windows, revise only if Git Bash discovery becomes part of a runner. |
| L014 | verified | Windows workspace shell | The workspace sandbox command helper may fail during setup refresh; a scoped elevated-shell command can be a temporary recovery path. Do not infer the cause or change project permissions from this incident. | T28: repeated `exec_command` starts returned `helper_unknown_error: setup refresh had errors`; scoped elevated-shell commands ran Git inspection, full local verification and visible headed E2E. T30 reproduced the same helper error; the scoped fallback enabled inspection, local tests and verify again. The Codex CUA kernel separately exited unexpectedly in T28/T30. | Use only as bounded recovery when the helper fails; reassess if the same fallback stops working. Do not add permission changes. |
