# Toolchain baseline

Verified on 2026-09-23 during T02.

| Tool | Version / policy |
| --- | --- |
| Node | 24.x; 24.15.0 available in the developer environment and 24.19.0 in the Codex runtime |
| npm | 11.12.1; repository package manager, bundled with the verified Node installation |
| React | 19.3.0 resolved in the lockfile |
| Vite | 8.3.1 resolved in the npm lockfile |
| TypeScript | 6.0.3 resolved in the lockfile |
| Phaser | 4.2.1 resolved in the lockfile |
| .NET SDK | 10.0.401 pinned by `global.json` |
| ASP.NET runtime packages | 10.0.12 generated by the installed template |

The developer Node installation exposes npm 11.12.1. The Codex runtime can expose a different Node binary, so use the repository `packageManager` field and `package-lock.json` as the reproducible npm baseline. No system `dotnet` is currently exposed; developers with the pinned SDK installed can use `dotnet` normally. Update versions through an approved dependency task and commit lockfile changes.
