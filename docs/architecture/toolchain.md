# Toolchain baseline

Verified on 2026-09-23 during T02.

| Tool | Version / policy |
| --- | --- |
| Node | 24.19.0 available in the Codex runtime |
| pnpm | 11.19.0; repository package manager |
| React | 19.3.0 resolved in the lockfile |
| Vite | 8.3.0 resolved in the lockfile |
| TypeScript | 6.0.3 resolved in the lockfile |
| Phaser | 4.2.1 resolved in the lockfile |
| .NET SDK | 10.0.401 pinned by `global.json` |
| ASP.NET runtime packages | 10.0.12 generated by the installed template |

The local machine did not expose a system `npm` or `dotnet`. This repository uses pnpm. The session installed .NET 10.0.401 in a temporary local tool directory outside the repository; developers with the pinned SDK installed can use `dotnet` normally. Update versions through an approved dependency task and commit lockfile changes.
